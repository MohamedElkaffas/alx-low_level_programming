#include "main.h"

/**
 * _strlen - returns length of string
 * @s: string
 * Return: length of string
 */

int _strlen(char *s)
{
	int ans = 0;

	while (s[len] != '\0')
	{
		++ans;
	}

	return (ans);
}

